{
  "name": "tn35_santos_cs0035finalproject",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:browser": "npm run build && esbuild src/browser.ts --bundle --format=esm --outfile=stitch_low_cortisol_frontend/compiler-bundle.js --sourcemap",
    "lowcortisol": "npm run build:browser && npm --prefix stitch_low_cortisol_frontend start",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
